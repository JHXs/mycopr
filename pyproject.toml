[project]
name = "copr-packages"
version = "0.1.0"
description = "Repo to manage COPR package updates and builds"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "copr-cli>=2.4",
]

[tool.uv]
exclude-newer = "3 days"
